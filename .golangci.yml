version: "2"

linters:
  enable:
    - errcheck
    - govet
    - ineffassign
    - misspell
    - revive
    - staticcheck
    - unused
  exclusions:
    rules:
      - linters:
          - revive
        text: "^exported:"
      - path: _test\.go$
        linters:
          - revive
        text: "^unused-parameter:"

formatters:
  enable:
    - gofmt
    - goimports
